# 愷樂生醫官網 — 上線前待填清單

> 全站已完成（33 個頁面、24 篇文章、完整 Schema）。
> 以下是上線前「需要你提供真實資料」才能填的項目。建議依 P0 → P3 順序處理。

---

## 🔴 P0｜部署上線 + 真實網域（不做＝全部白做）

### 1. 部署到 Cloudflare Pages
- 整個「凱樂生醫 委外網站」資料夾可直接上傳。
- 純靜態 HTML，無需 build。
- 步驟：Cloudflare 後台 → Workers & Pages → Create → Pages → 上傳資料夾。

### 2. 真實網域
目前全站用 `https://www.kailebio.com`（佔位）。
拿到真實網域後，**全站搜尋取代**：
```
搜尋：https://www.kailebio.com
取代：https://你的真實網域
```
影響檔案：所有 .html 的 canonical、og:url、sitemap.xml、robots.txt。
（可用下方「批次取代指令」一次完成。）

---

## 🔴 P1｜真實聯絡入口（直接決定成交）

### 3. LINE 官方帳號連結 ★最重要
目前全站 LINE 連結都是假的 `https://line.me/`（點了沒反應）。
請提供 LINE 官方帳號連結（例如 `https://lin.ee/xxxxxx`），全站搜尋取代：
```
搜尋：https://line.me/
取代：你的真實 LINE 連結
```
※ 出現位置：每頁頂部列、Hero、聯絡區、浮動按鈕、頁尾，共 30+ 處。

### 4. 確認電話 / Email（已填，請核對）
- 電話：`0963-339-098`（tel:0963339098）
- Email：`lawrenceyu911@gmail.com`
- 如有誤，搜尋上述字串取代即可。

### 5. Google 地圖
目前用免費 embed（地址已編碼）。上線後若顯示空白，可改用
Google Maps「分享 → 嵌入地圖」產生的 iframe 取代 `fora.html` / `index.html` 內的地圖 iframe。

---

## 🟠 P2｜搜尋曝光（決定 Google/AI 找不找得到）

### 6. Google Search Console 驗證碼
全站搜尋取代：
```
搜尋：REPLACE_WITH_YOUR_VERIFICATION_CODE
取代：你的 GSC 驗證碼
```
上線後到 Search Console 提交 `sitemap.xml`。

### 7. Google Analytics 4（GA4）
編輯每頁底部的 GA 區塊（目前註解狀態），或集中放一段。
```
搜尋：G-XXXXXXXXXX
取代：你的 GA4 評估 ID
```
（目前 GA 程式碼在 index.html 底部是「註解」狀態，填好後需取消註解。）

---

## 🟡 P3｜分享第一印象（已先用 SVG 頂著）

### 8. Logo / OG 分享圖（已建立暫代版）
- 暫代檔：`assets/img/logo.svg`、`assets/img/og-cover.svg`（純文字版，不會破圖）。
- 建議日後換成正式設計的 Logo 與 1200×630 OG 圖（可保留 .svg 或改 .png/.jpg，
  若改副檔名記得同步更新 html 內 `og:image` 與 schema 的 logo 路徑）。

---

## ⚪ 之後可漸進補（不急）

- 文章從 24 篇補到 30 篇（建議上線後看 Search Console 數據再決定主題）。
- 產品實拍照（目前用 emoji 佔位）。
- 真實客戶見證（目前為範例文案）。

---

## 🛠 批次取代指令（拿到真實資料後，一次完成）

在本資料夾開啟 Git Bash，把下面 `你的XXX` 換成真實值後執行：

```bash
cd "C:/Users/opopw/OneDrive/桌面/凱樂生醫 委外網站"

# LINE 連結
sed -i 's#https://line.me/#你的LINE連結#g' *.html

# 真實網域（注意 # 分隔避免斜線衝突）
sed -i 's#https://www.kailebio.com#https://你的網域#g' *.html sitemap.xml robots.txt

# GSC 驗證碼
sed -i 's#REPLACE_WITH_YOUR_VERIFICATION_CODE#你的驗證碼#g' *.html

# GA4 ID
sed -i 's#G-XXXXXXXXXX#你的GA4_ID#g' *.html
```

> 執行前建議先複製整個資料夾備份。
> 不確定的話把真實資料給我，我可以幫你執行並驗證。
